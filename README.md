# similar-image-spider
For building a dataset to obtain similar images.

### support
- search engines
    - baidu
    - google

- proxy
    - http & https
    - auto update 
    - fastapi server
```shell
# use
cd proxy/
python proxy_api.py

# curl http://localhost:8000/proxy
```